model User {
  id        Int      @id @default(autoincrement())
  email     String   @unique
  name      String
  status    Boolean
  createdAt DateTime
  updatedAt DateTime
  password  String?

  @@map("users")
}
