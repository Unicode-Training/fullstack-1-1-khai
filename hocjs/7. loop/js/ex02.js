// let html = "";

// let number = 15;
// for (let i = 1; i <= number; i++) {
//   //Toán tử 3 ngôi
//   const className = i % 2 === 0 ? `class="active"` : "";
//   html = html + `<h2 ${className}>Học lập trình không khó ${i}</h2>`;
// }

// //Output: Chuỗi html

// //Muốn hiển thị chuỗi html lên trình duyệt --> Dùng DOM

// document.body.innerHTML = html;

//Cho trước số hàng và số cột --> Vẽ bàn cờ vua từ số hàng và cột cho trước
let rows = 8;
let cols = 8;
