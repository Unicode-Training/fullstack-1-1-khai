# Chạy NodeJS trên TypeScript

## Cách 1

- Dùng lệnh tsc để build ra file js
- Chạy file js thông qua node

## Cách 2

- Dùng tsx để chạy trực tiếp file ts
- Nếu sử dụng es module => cần phải thêm .js phía sau lệnh import
