# NodeJS

- Môi trường chạy JS ngoài trình duyệt
- Cách chạy: `node duong-dan-toi-file-js`

## Đặc điểm

- Không hỗ trợ các API bên trình duyệt: DOM, localStorage,..
- Cho phép kết nối với các server bên ngoài: Database, Redis,...
- Thao tác với hệ thống file trên server
- Mặc định hỗ trợ module kiểu CommonJS (require)
- Mặc định nodejs sẽ hỗ trợ sẵn 1 số module (Không cần cài)

## Các module phổ biến

1. File System (Thao tác với file)

```js
import fs from "fs";
```
